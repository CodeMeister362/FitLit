class newData {
  constructor(currentUserId, inputDate, inputNumSteps, inputMinActive, inputStairs) {
    this.userID = currentUserId
    this.date = inputDate
    this.numSteps = inputNumSteps
    this.minutesActive = inputMinActive
    this.flightsOfStairs = inputStairs
  }
}

export default newData;